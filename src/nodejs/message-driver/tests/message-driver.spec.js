const MessageDriver = require('../src/message-driver.js');

describe('Message Driver', () => {
  describe('Message Driver', () => {
    it('should import without error', () => {
      expect(MessageDriver).toBeTruthy();
    });
  });
});
