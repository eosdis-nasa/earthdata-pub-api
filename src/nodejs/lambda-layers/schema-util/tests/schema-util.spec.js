const SchemaUtil = require('../src/schema-util.js');

describe('Schema Util', () => {
  describe('Schema Util', () => {
    it('should import without error', () => {
      expect(SchemaUtil).toBeTruthy();
    });
  });
});
